TASK — BATCH A (Milestones M1 + M2): project scaffold, renderer, camera, lights, sand.

Produce a COMPLETE, runnable Vite + TypeScript + Three.js project skeleton with the sand
plane rendered and textured. Implement these files (project root = sand-footprint-sim/):

1. package.json — name "sand-footprint-sim", type module, scripts: dev/build/preview ("vite",
   "tsc --noEmit && vite build", "vite preview"), and a "typecheck": "tsc --noEmit".
   deps: three (latest), lil-gui. devDeps: typescript, vite, @types/three (matching three).
   Pin reasonable recent versions (three ^0.169 or newer that has WebGPURenderer + TSL).
2. tsconfig.json — strict, moduleResolution bundler, target ES2022, DOM libs, noEmit.
3. vite.config.ts — minimal; base "./" so it works from a subpath.
4. index.html — full-screen canvas mount, links src/main.ts, includes a "Wash" button and a
   container for lil-gui. Mobile viewport meta, touch-action none on body.
5. src/styles.css — full-screen layout, no scroll, touch-action:none, Wash button styling
   (fixed bottom-center), gui container top-left.
6. src/scene/createRenderer.ts — async createRenderer(canvas): tries WebGPURenderer
   (three/webgpu) and .init(); on failure falls back to WebGLRenderer (WebGL2, antialias).
   Set ACESFilmicToneMapping, sRGB output, pixel ratio capped at ~2. Return { renderer, backend }.
   Log which backend was selected.
7. src/scene/createCamera.ts — OrthographicCamera looking straight down (top-down) at XZ plane.
   Provide resize(w,h) keeping the sand filling the screen, worldHeight() for the visible world
   height (used so footprint size ≈ 10vh), and screenToWorld(px,py) -> THREE.Vector2 (XZ).
8. src/scene/createLights.ts — DirectionalLight (sun) from an oblique angle so depressions cast
   shadow under the top-down camera, plus Ambient/Hemisphere fill. Soft PCF shadows (modest map size).
9. src/sand/sandParams.ts — a SandParams type + default values object with ALL 11 GUI params from
   DESIGN.md §6 (moisture, hardness, grainSize, cohesion, bodyWeight, footprintDepth, rimHeight,
   edgeCollapse, decaySpeed, darkeningStrength, noiseStrength). Sensible defaults in [0,1]-ish ranges.
10. src/sand/sandShader.ts — builds the sand material. Prefer TSL node material (works on WebGPU and
    WebGL2). Procedural fbm/value noise drives micro normal perturbation, subtle albedo and roughness
    variation; moisture darkens/desaturates and lowers roughness. Expose uniforms/nodes for the params
    and (placeholder for now) a height/displacement input + wash front uniform. Keep it readable.
11. src/sand/SandPlane.ts — class SandPlane: mid-density PlaneGeometry (e.g. 256x256) on XZ plane,
    uses the sand material. For Batch A expose an update(dt, params) that pushes params to the material.
    Leave clearly-marked hooks (methods/fields) for the HeightField displacement to be filled in Batch B
    (e.g. a public heightField placeholder, applyFootprints(...) stub, applyWash(front) stub).
12. src/main.ts — boot: create renderer (await), camera, lights, SandPlane, a basic resize handler,
    and a requestAnimationFrame loop calling sandPlane.update + render. No footprints/GUI/wash yet
    (those are later batches) but leave a clean place to add them. Must run with no console errors.

Keep everything consistent with DESIGN.md §13 file layout and §14 API sketch. This batch must
`npm install` + `npm run typecheck` + `vite build` cleanly. Output all 12 files now.
